class Node {
  Node next;

  void setNext (Node n) { next = n; }
}

class IntQueue {
  Node front;
  Node rear;

  boolean isEmpty () { return front == null; }

  void insert (int x) {
    Node last = new Node;
    if (self.isEmpty()) {
      printString("first");
    }
    else {
      printString("other");
    }
  }
}

int main () {
  IntQueue q = new IntQueue;
  q.insert(12);

  return 0;
}

     